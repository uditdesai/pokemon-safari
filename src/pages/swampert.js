import * as React from "react";
import PokemonPage from "../components/pokemonPage";

// markup
const Swampert = () => {
  return (
    <>
      <PokemonPage pokemonName="swampert" />
    </>
  );
};

export default Swampert;
